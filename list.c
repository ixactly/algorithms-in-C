/*
 * - advantage on using linked-list
 * easily insert(and delete) value (when we insert value between indices of array,
 * we do not need to move value to next one by one)
 * - disadvantage
 * hard to search n-th index value (too late(O(n))) because we have to chase linked list from head
 * of the list to n-th index (list don't have index)
 */

int main() {}